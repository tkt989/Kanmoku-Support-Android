<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.tkt989.kanmokusupport">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ShowIllustActivity"
            android:theme="@style/FullscreenTheme"></activity>
        <activity android:name=".activities.IllustSelectActivity" />
        <activity
            android:name=".activities.ShowDrawingActivity"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name=".activities.DrawingActivity"
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>